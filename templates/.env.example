# Copy to repo root as `.env` (gitignored) and fill real values.
# install moves GitHub MCP tokens to ~/.config/github-mcp/env for Claude/Codex.

# GitHub PAT for github MCP (fine-grained; repos/issues/pull_requests read).
GITHUB_PERSONAL_ACCESS_TOKEN=
